#include <iostream>

#include "Gameplay.h"
#include "Menu.h"
#include "Fleet.h"

int main()
{
	Gameplay g;
	//g.startGame();
	g.playGame();

	//int index = 0;

	//Board b;
	//b.deploymentPlaceShip(0, 0, 0, false);
	//b.deploymentPlaceShip(3, 0, 2, false);
	//b.shootingCheckHit(0, 0);
	//b.shootingCheckHit(1, 0);
	//b.shootingCheckHit(2, 0);
	//b.shootingCheckHit(3, 0);

	//b.shootingCheckHit(0, 2);
	//b.shootingCheckHit(1, 2);
	//b.printAll();



	//f.checkHit(index, 0, 0);
	//f.checkHit(index, 1, 0);
	//f.checkHit(index, 2, 0);


	//Menu m;
	//m.selectGameMode();

	return 0;
}